/**
 * routeConfig.js
 * Centralised route path constants for the Investors module.
 */

const BASE = '/investors';

export const ROUTES = {
  DASHBOARD:           `${BASE}/dashboard`,
  NEW_INVESTMENT:      `${BASE}/new-investment`,
  CUSTOMER_ALLOCATION: `${BASE}/customer-allocation`,
  PROFILE:             `${BASE}/profile`,
  LOGIN:               `${BASE}/login`,
  LOGOUT:              `${BASE}/logout`,
};
